/**
 * @klaim/contracts — shared API request/response contracts for KLAIM V2.
 *
 * Depends on @klaim/types. No business logic; contracts + lightweight validators.
 */
export * from "./verification-requests";
