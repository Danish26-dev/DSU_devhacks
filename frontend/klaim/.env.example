# KLAIM Identity frontend — environment reference (Vite).
# Copy to .env.local for local development. Never commit real secrets.

# Base URL of the KLAIM API (backend/klaim-api). No trailing slash needed.
VITE_KLAIM_API_URL=http://localhost:8080
